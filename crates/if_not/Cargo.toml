[package]
name = "logline-if-not"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LogLine if_not slot: rejection, falsification, and problem marking."

[lib]
name = "logline_if_not"
path = "src/lib.rs"

[dependencies]
logline-who = { path = "../who" }
serde.workspace = true

[lints]
workspace = true
